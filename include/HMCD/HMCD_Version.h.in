#define HMCD_VERSION_MAJOR @HMCD_VERSION_MAJOR@
#define HMCD_VERSION_MINOR @HMCD_VERSION_MINOR@
#define HMCD_VERSION_PATCH @HMCD_VERSION_PATCH@
#define RELEASE_BUILD @RELEASE_BUILD@

#ifdef RELEASE_BUILD
    #define NDEBUG
#endif

#ifdef __WIN32
    #define __FILENAME__ (__FILE__ + @SRC_PATH_SIZE@)
#else
    #define __FILENAME__ (__FILE__ + @SRC_PATH_SIZE@)
#endif